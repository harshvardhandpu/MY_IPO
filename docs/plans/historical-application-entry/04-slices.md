# Vertical Slices: Historical application entry

1. **Tracer: owner historical application end to end** — failing service and frontend flow checks first; add the minimal typed command, event-chain write, projection migration, and native form needed to save one owner-affirmed historical application and return safe opaque ids.
2. **Security and provenance boundaries** — add focused failures for missing affirmation, non-owner account, invalid amount/date/issue id, PAN-shaped public fields, schema sensitivity, and confirm no job/result/provider request is created.
3. **Real owner entry and verification** — build/install current desktop, use the native historical form for Symbiotec ₹14,820 with unknown date and MUFG issue `11926`, then inspect the safe projection/event state and run canonical gates.
